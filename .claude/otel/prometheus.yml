# Prometheus pulls the numbers from the collector every 10 seconds.
global:
  scrape_interval: 10s

scrape_configs:
  - job_name: "claude-code"
    static_configs:
      - targets: ["otel-collector:8889"]
